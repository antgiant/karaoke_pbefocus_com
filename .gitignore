.DS_Store
__pycache__/
node_modules/

# The actual song library (private, not yet cleared for public release) and
# its Bible-text reference source. These stay local/private-host-only -- the
# whole point of the app's manifest-URL gate is that this content never
# lives in the public code repo. See AGENTS.md for the pipeline that
# produces/consumes these.
/PBE_2026_2027/
/NEW KING JAMES VERSION.json

# Local pipeline run logs/reports -- regenerated by scripts/*.py, not source.
scripts/logs/

# Generated manifests contain the private, not-yet-cleared song content
# (lyrics + timing). Never commit or publish these.
scripts/dev-manifest.local.json
scripts/*.local.json
